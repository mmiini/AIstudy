#패키지 임포트


##데이터 불러오기##
#Dataset 설정
#Dataset으로 부터 DataLoadr 만들어야 함

#모델 만들기
    #모델 설계도 만들기
    #모델 생성에 사용이 될 Hparm 설정
    #모델 객체를 생성 (설계도 + Hparam)


# Loss 계산하는 계산기

# Optimizer

# for loop를 돌면서 데이터 불러오기
    #불러온 데이터 모델에 넣기

    #나온 출력물로 loss를 계산
    #loss로 back prop 진행
    #optimizer를 이용해 최적화를 진행해야함

    #학습 중간에 평가를 진행해서
    #성능이 좋으면 저장을 진행




#패키지 임포트

#GPU 설정

#데이터 불러오기
#Dataset만들기
#DataLoader만들기

#모델
    #모델 설계도
    #모델 생성에 사용이 될 Hparam 설정
    #모델 객체 설정(설계도 + Hparm)

# Loss 계산
# optimizer 계산
# 에폭수

#for loop 돌면서 데이터 불러오기
    # 데이터 모델에 넣기

    #옵티마이저 내 기울기 초기화
    #순전파
    #loss값 계산
    #손실 추가
    #loss로 back prop 진행
    #가중치 갱신

    #학습 중간에 평가를 진행해서
    #성능이 좋으면 저장을 진행